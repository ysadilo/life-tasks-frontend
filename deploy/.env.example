# Copy to deploy/.env on the Pi. Never commit the real file.
# Cloudflare Zero Trust → Networks → Tunnels → your tunnel → "Install connector"
# shows the token (the long string after `--token`).
TUNNEL_TOKEN=eyJ...
